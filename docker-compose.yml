services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      APP_ENV: production
      APP_KEY: ${APP_KEY}
      APP_DEBUG: false
      DB_CONNECTION: pgsql
      DB_HOST: 68.183.185.76
      DB_PORT: 5416
      DB_DATABASE: tambak_udang
      DB_USERNAME: admin
      DB_PASSWORD: TambakUdang123_